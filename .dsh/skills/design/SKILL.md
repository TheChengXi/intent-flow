---
name: design
description: 为需求做结构设计，输出模块划分和接口边界。新项目从零设计，已有项目顺应原有结构。
---

# 结构设计

以下规则具有最高优先级。

## 核心规则

跨层依赖是 bug。结构设计阶段强制排除，不允许进入编码阶段。

依赖方向只有一种：上层可依赖下层，下层绝不能依赖上层，不允许跨层依赖。

---

## 执行原则

动手前先读现有结构，跟随既有分层与命名；无既有结构 → 按三层划分模块。

新增依赖不得跨层；既有跨层依赖 → 标入 design.md 最小依赖链，列入本次一并修复。

拍板不了 → 联网搜索架构方案。

### 产出

- `.intentflow/<feature-name>/design.md` — 本次设计文档（必须完成，模板见下）
- `.intentflow/<feature-name>/later-on.md` — 后续想法备忘（模板见下）

---

## 本次必须完成 — design.md

### 0. 与需求文档的偏差（设计阶段新发现）

需求分析阶段没发现、设计阶段验证时暴露的事实。逐条记录。

- **偏差**：[发现什么] — **影响**：[设计如何调整]

### 1. 模块清单

- **[模块名]**：[层级] — 职责：[职责] — 依赖：[依赖关系]

### 2. 最小依赖链

从入口到数据层跑通本次需求的关键路径和接口：

[高层] → [中间层] → [低层]

### 3. 测试策略

- **验证方式**：每个模块标注如何验证 — 理由：[肉眼/类型可验证 / 需运行时行为验证]
- **验证命令**：[验证项]：[命令] — 预期：[结果]

### 4. 决策记录

只记录值得记录的决策，不值得记录的直接实施，不写进文档。判定标准——至少占一条就值得记录：

- **难逆转**：改起来伤筋动骨
- **无上下文难理解**：后来者看到会问"为什么这么干"
- **有真实权衡**：选 A 必然放弃 B

每条格式：

- **决策**：[做了什么选择]
- **理由**：[为什么选它；对比过的备选项与取舍]
- **影响**：[对后续模块/编码的约束，若有]

模块归属与拆分理由属于决策记录范畴，在对应决策条中说明。

### 5. 改动点清单

- 改动点 + 新增文件（无新增写"无"）

---

## 后续想法备忘 — later-on.md

> 设计阶段识别但**此时不做**的事项，以及未来可能的演进方向。只记录想法，不做任何设计预留——需要时直接实现。

### 想法列表

- **L01**：[想法标题]
  - 现状：[现在是什么状态 / 为什么现在不做]
  - 何时做：[触发条件]

### 与当前设计的关系

想法可能影响当前模块的接口或分层，但当前不做预留——届时直接改。
