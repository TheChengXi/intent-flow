# 架构约束（所有设计分支通用）

无论修改还是新增，所有设计必须遵守以下约束：

## 分层架构

- 严格三层架构：adapter layer → application layer → data layer
- 外层可以依赖内层，内层绝不能依赖外层
- 不能跨层依赖

## 依赖倒置（DIP）

- 内层定义接口（抽象），外层实现接口
- 如果业务上有跨层需求，必须在中间层作为抽象接口

## 设计原则

- 接口定义遵循最小暴露原则：只暴露必要的，隐藏实现细节
- 模块职责单一：一个模块只做一件事
- 不做设计预留：不为「以后可能用到」的功能提前设计接口
