# CDD Framework 测试指南

## 测试环境准备

1. **配置 API Key**
   - 打开 VSCode 设置
   - 搜索 `cdd.apiKey`
   - 填入你的 Claude API Key
   - （可选）配置 `cdd.apiBaseUrl` 和 `cdd.modelId`

2. **打开测试文件**
   - `test-sample.ts` - TypeScript 测试
   - `test-sample.py` - Python 测试
   - `test-sample.go` - Go 测试

## 测试场景

### 场景 1: 编译功能测试

**目标**: 验证从注释生成代码的功能

**步骤**:
1. 打开 `test-sample.ts`
2. 选中"测试 1"的注释部分（从 `// @contract: add` 到最后一个 `// @boundary`）
3. 右键 → `CDD: 编译注释为代码`
4. 等待进度条完成

**预期结果**:
- 在注释下方生成 `add` 函数的实现
- 函数应该包含参数类型检查
- 函数应该返回 `a + b`
- 函数末尾应该有 `// @end` 标记
- 显示提示："编译完成：add"
- 自动触发审查流程

**验证点**:
- 生成的代码是否符合注释描述
- 是否处理了所有 @boundary 条件
- 是否自动触发了审查

---

### 场景 2: 审查功能测试（发现问题）

**目标**: 验证审查员能发现代码与注释不一致

**步骤**:
1. 打开 `test-sample.ts`
2. 选中"测试 2"的完整代码块（从 `// @contract: validateUser` 到 `// @end`）
3. 右键 → `CDD: 审查代码`
4. 等待进度条完成

**预期结果**:
- 审查员发现缺少密码长度检查
- 结论应该是 `MAJOR_VIOLATION`
- 弹出裁决对话框，显示：
  - 路径A（注释为准）：重新编译
  - 路径B（代码为准）：反向同步
- 生成审查报告追加到 `REVIEW_REPORT.md`

**验证点**:
- 是否正确识别缺少的 @step 实现
- 是否触发裁决流程
- 审查报告是否包含具体问题描述

---

### 场景 3: 转译功能测试

**目标**: 验证从代码生成注释的功能

**步骤**:
1. 打开 `test-sample.ts`
2. 选中"测试 3"的 `calculateDiscount` 函数（整个函数体，不包括注释）
3. 右键 → `CDD: 转译代码为注释`
4. 等待进度条完成

**预期结果**:
- 在函数上方生成 CDD 格式注释
- 注释应该包含：
  - `@contract` 行（函数签名）
  - 多个 `@step` 行（描述实现步骤）
  - 多个 `@boundary` 行（描述边界条件处理）
- 显示提示："注释已生成，请人工审查后再编译"

**验证点**:
- 生成的 @contract 是否正确
- @step 是否覆盖主要逻辑
- @boundary 是否包含错误处理

---

### 场景 4: 完整流程测试（编译 → 审查 → 修复 → 重新编译）

**目标**: 验证完整的开发循环

**步骤**:
1. 打开 `test-sample.ts`
2. 选中"测试 4"的注释部分
3. 执行 `CDD: 编译注释为代码`
4. 等待代码生成完成
5. 选中生成的完整代码块（包括注释和代码）
6. 执行 `CDD: 审查代码`
7. 如果审查不通过：
   - 根据审查反馈修改注释
   - 重新选中注释部分
   - 再次执行编译
8. 重复直到审查通过

**预期结果**:
- 第一次编译生成代码
- 审查可能发现问题
- 修改注释后重新编译时，编译器应该能看到审查反馈
- 最终审查通过

**验证点**:
- 编译器是否使用了审查反馈
- 历史记录是否正确保存
- 多次编译是否保留历史

---

### 场景 5: 多语言支持测试

**目标**: 验证 Python 和 Go 的支持

**步骤**:
1. 打开 `test-sample.py`
2. 选中"测试 1"的注释
3. 执行编译
4. 验证生成的 Python 代码

5. 打开 `test-sample.go`
6. 选中"测试 1"的注释
7. 执行编译
8. 验证生成的 Go 代码

**预期结果**:
- Python 代码使用正确的语法（缩进、类型提示）
- Go 代码使用正确的语法（类型声明、错误处理）
- 两种语言都能正确解析和生成

**验证点**:
- 语法是否符合语言规范
- 类型系统是否正确处理
- 错误处理是否符合语言习惯

---

### 场景 6: 历史记录验证

**目标**: 验证历史记录的保存和清理策略

**步骤**:
1. 完成上述所有测试
2. 打开项目根目录的 `.cdd/history/` 文件夹
3. 查看生成的历史记录文件

**预期结果**:
- 每个函数有独立的 JSON 文件
- 文件路径：`.cdd/history/文件路径/函数名.json`
- 文件内容包含：
  - `contract`: 契约版本
  - `records`: 记录数组
    - `compiler` 记录（最多 10 条）
    - `reviewer` 记录（所有失败 + 最近 5 条通过）
    - `translator` 记录（最多 5 条）

**验证点**:
- 历史文件是否正确创建
- 记录格式是否正确
- 清理策略是否生效

---

## 常见问题排查

### 问题 1: "未找到 @contract，无法编译"
- 确保选中了包含 `@contract` 的注释
- 注释格式必须正确：`// @contract: functionName(...) => returnType`

### 问题 2: "代码块不完整：缺少 // @end"
- 审查时必须选中完整代码块
- 确保代码末尾有 `// @end` 标记

### 问题 3: "请先配置 API Key"
- 打开 VSCode 设置
- 配置 `cdd.apiKey`

### 问题 4: 编译后没有自动触发审查
- 检查 CompileCommand.ts 是否调用了 ReviewCommand.execute()
- 查看控制台是否有错误信息

### 问题 5: 历史记录没有保存
- 检查 `.cdd/history/` 目录是否存在
- 查看控制台是否有文件写入错误

---

## 测试检查清单

- [ ] TypeScript 编译功能正常
- [ ] Python 编译功能正常
- [ ] Go 编译功能正常
- [ ] 审查功能能发现问题
- [ ] 审查通过时正常结束
- [ ] 审查不通过时触发裁决
- [ ] 转译功能生成正确注释
- [ ] 编译后自动触发审查
- [ ] 审查反馈能传递给编译器
- [ ] 历史记录正确保存
- [ ] 历史清理策略生效
- [ ] 多次编译保留历史
- [ ] COMPILE_SPEC.md 正确加载
- [ ] 工作日志正确记录

---

## 下一步

测试完成后，可以实现：
1. 跨文件引用功能（读取被调用函数的契约）
2. 更多语言支持（Java、C++、Rust 等）
3. 更智能的审查维度
4. 更好的错误提示
