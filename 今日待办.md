好的。这是你睡醒后要面对的任务清单，和整理后的草案。

---

## 今日待办

**第一优先：重构 VM，剥离交接逻辑**

编译器、审查员、转译员的 VM 里，目前可能残留着“编译完自动调用审查员”这类交接逻辑。今天要把它们从 VM 里剥离出来，让 VM 回归纯粹——只负责接收上下文、调用 AI API、返回结果。

交接逻辑剥离后，VM 的 `@contract` 会变得更简洁。这件事必须在实现 Workflow 之前做，否则 Workflow 调度器会和 VM 内部的残留交接逻辑冲突。

**第二优先：实现 `executeCDDWorkflow`**

这个函数是 CDD 调度器的核心入口。它负责按 CDD 的规则，串联编译器、审查员、转译员，处理编译-审查轮巡和不通过时的回溯。

用 CDD 的方式开发它：先写 `@contract` 和 `@step`，再编译，再审查。

**第三优先：整理 2.5 草案，写进 `_source/`**

将增量编译与局部审查机制写进 `COMPILE_SPEC.md` 或新建 `INCREMENTAL_COMPILE.md`。这是 CDD 的“省钱发动机”，不需要立即在代码里实现所有细节，但需要完成注释层的设计：为关键路径的 `@step` 补充 `@step-id`。

**第四优先：测试自举闭环**

当 `executeCDDWorkflow` 被审查通过后，用 CDD 自身来编译 CDD 的某个函数。在编辑器里右键选择“CDD: 编译并审查当前工作行”，不再手动拼装提示词。这是 CDD 完全自举的标志。

---

## CDD 增量编译与局部审查机制（草案 v2.5.0）

**状态：** 草案，待落地后整合

**定位：** 核心铁律级别算法规则，非角色提示词。机制本身不依赖特定 AI 服务商，任何模型均可适用。

**思路来源：** 本机制灵感来自 DeepSeek 上下文缓存（KV Cache）的设计哲学——“重复的前缀何必重算”。在与其探讨 API 成本优化时，发现这套“找相同、算差异”的工程逻辑恰好能解决 CDD 修改函数时的冗余问题。特此致意。


### 一、机制目标

当函数仅有局部 `@step` 发生变更时，避免对整个函数进行全量编译与全量审查。通过“仅发送差异，仅计算差异”的方式，在架构层面砍掉冗余计算量。

核心理念：**既然只改了这里，何必重算全部？**


### 二、@step-id 标记规则

1.  关键路径的每个 `@step` 必须附加唯一 ID，格式：`// @step: [模块缩写-序号] [意图描述]`
2.  `@step-id` 与步骤意图永久绑定。即使物理顺序调整，ID 不变
3.  标记为 `// @simple` 的步骤无需 ID


### 三、增量编译触发条件

**触发增量编译（满足以下全部条件）：**
- `@contract` 未发生任何变化
- 本次变更仅限于修改、增加或删除带有 `@step-id` 的步骤

**必须全量编译（满足以下任一条件）：**
- `@contract` 变更
- 新增函数
- 修改了 `@step-id` 之外的注释（如函数头部描述）


### 四、编译器增量行为

1.  用 `// @diff-start` 和 `// @diff-end` 包裹受影响的代码块
2.  未变更部分用占位符 `// ... (unchanged logic from previous compilation)` 表示
3.  若增量修改导致函数预计超 200 行，仍须暂停建议拆分


### 五、审查员增量行为

1.  仅审查 `@diff` 区间代码及其与前后未变更代码的衔接处
2.  对未变更的 `@step-id`，在审查报告中标记 `[缓存审查通过]`，跳过重复审计
3.  审查不通过时打回编译器重试增量输出，或由人决策降级为全量重编译


### 六、与现有规则兼容性

本机制与平等事实原则、回溯机制、反屎山原则、强制工作日志均兼容，不冲突。工作日志在增量模式时需注明模式和受影响步骤。


### 七、与 DeepSeek KV Cache 的关系

两者相互独立、相互叠加。CDD 增量编译在客户端主动标记差异、从源头减少发送量；DS 缓存由服务端自动识别重复前缀、跳过重算。一个是源头节流，一个是服务端加速，共同实现“改哪算哪”的工程直觉。

---