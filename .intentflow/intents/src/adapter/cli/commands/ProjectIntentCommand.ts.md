# ProjectIntentCommand.ts

`src/adapter/cli/commands/ProjectIntentCommand.ts`

**intent:** 将原始 CLI 参数翻译为 ProjectIntentUseCase 的输入协定。定义 CLI 特有逻辑：intent 为必填 --intent 参数，force 支持 --force 标志和 --force=true 两种写法
