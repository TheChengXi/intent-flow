# 本次必须完成 — 架构设计

## 模块清单
- **[模块名]**：[层级] — 职责：[职责] — 依赖：[依赖关系]

## 最小依赖链
从入口到数据层跑通本次需求的关键路径和接口：

[高层] → [层级] → [低层]

## 测试策略

### 验证模式
- **[模块/文件]**：[直接模式 / 隔离 TDD] — 理由：[肉眼/类型可验证 / 需运行时行为验证]

### 依赖注入点
- **[模块]**：[构造器/参数] 注入 [依赖]，不在内部创建

### 验证命令
- **[验证项]**：[命令] — 预期：[结果]

### Mock 边界
- 只 mock 系统边界（外部服务、IO），不 mock 内部协作者

## 本次设计决策
- 为什么这些模块归到这些层
- 接口定义的关键约束
- 当前不做的取舍（显式记录本次需求范围外的内容，但不做任何设计预留）