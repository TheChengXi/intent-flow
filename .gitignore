# WASM 编译产物 — 从 node_modules/tree-sitter-wasms 复制
src/data/services/core/parsers/

# 防止 Git Bash 环境下误将 > nul 重定向为文件名
nul

# VSCode
.vscode/
*.vsix

# 构建产物
dist/

# 依赖
node_modules/
# MCP构建
.mcp.json
# DSH 工作区局部 MCP 补丁（不入仓库）
.dsh/dsh.mcp.patch.yml

# 本地部署脚本（不入仓库）
scripts/*
# Release 打包临时目录
release-build/
fluted-glass.glsl
stop-dsh.bat
start-dsh.bat
