# 现在必须完成 — 架构设计

## Phase 1 模块清单

| 模块 | 层级 | 职责 | 依赖的 Phase 1 模块 | 为哪些 Phase 2+ 模块预留了扩展口 |
|------|------|------|---------------------|--------------------------------|

## 最小依赖链

从入口到数据层跑通的最小路径，标注关键接口：

```
[适配器] → [用例] → [仓库接口]
   ↓          ↓          ↓
  Phase 1  Phase 1    Phase 1（findById）
                      预留（create / update / delete）
```

## 扩展预留声明

Phase 1 的每个模块，列出它为后续内容预留的扩展点：

| 模块 | 预留的扩展点 | 对应的 Phase 2+ 模块 |
|------|-------------|---------------------|

## 风险和边界

- 如果 Phase 1 跑不通，是卡在哪个接口？
- Phase 2+ 依赖的预留接口是否足够明确？
