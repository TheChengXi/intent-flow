# 本次必须完成 — 架构设计

## 模块清单
| 模块 | 层级 | 职责 | 依赖关系 |
|------|------|------|----------|

## 最小依赖链
从入口到数据层跑通本次需求的关键路径和接口：

[适配器] → [用例] → [仓库接口]

## 本次设计决策
- 为什么这些模块归到这些层
- 接口定义的关键约束
- 当前不做的取舍（显式记录本次需求范围外的内容，但不做任何设计预留）